import React from "react";
import styles from "./Header.module.css";

const Header = () => (
  <header>
    <h1 className={styles.title}>Welcome to Popcorn Time</h1>
  </header>
);

export default Header;
